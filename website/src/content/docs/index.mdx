---
title: slate
description: Type-safe Gleam wrapper for Erlang DETS (Disk Erlang Term Storage).
template: splash
editUrl: false
lastUpdated: false
hero:
  tagline: Persistent key-value storage for Gleam, built on Erlang DETS. No separate database service is required.
  image:
    file: ../../assets/slate.webp
    alt: Chalk drawing on a slate chalkboard of a database cylinder under a magnifying glass, with the slate logo above it.
  actions:
    - text: Get started
      link: /quick-start/
      icon: right-arrow
      variant: primary
    - text: What is slate?
      link: /introduction/
      variant: secondary
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';

## Start here

```sh
gleam add slate
```

slate supports Gleam's Erlang target. DETS is part of OTP; slate does not support the JavaScript target.

## What it looks like

Open a table, insert a value, and look it up. Reads use your decoders to check the stored data.

Create the parent directory before you run the example:

```sh
mkdir -p data
```

```gleam
import gleam/dynamic/decode
import gleam/result
import slate
import slate/set

pub fn main() {
  use users <- set.with_table(
    "data/users.dets",
    repair: slate.AutoRepair,
    access: slate.ReadWrite,
    key_decoder: decode.string,
    value_decoder: decode.int,
  )
  use Nil <- result.try(
    set.insert(users, "alice", 42),
  )
  set.lookup(users, key: "alice")
}
// Ok(42), and the table is closed
```

Gleam's `use value <- function(...)` syntax passes the rest of the block as a callback. Here, `with_table` supplies `users` and attempts to close the table when the callback returns. The decoders define the key and value types expected from disk; `result.try` stops early if the insert fails.

Stored data persists across node restarts. An abnormal node shutdown can lose pending writes and leave the file needing repair. The [quick start](/quick-start/) explains this example. See the [table cleanup guide](/advanced/with-table/) for cleanup and repair options.

## When to use DETS

<table class="slate-comparison">
	<caption class="sr-only">Storage approaches at a glance</caption>
	<thead>
		<tr>
			<th scope="col">Approach</th>
			<th scope="col">Setup</th>
			<th scope="col">Persistence</th>
			<th scope="col">Query capability</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>JSON file</td>
			<td data-label="Setup">File I/O and serialization</td>
			<td data-label="Persistence">Yes</td>
			<td data-label="Queries">None</td>
		</tr>
		<tr>
			<td><strong>DETS (slate)</strong></td>
			<td data-label="Setup"><strong>Included in OTP; add slate for Gleam</strong></td>
			<td data-label="Persistence"><strong>Yes</strong></td>
			<td data-label="Queries"><strong>Key lookup, fold</strong></td>
		</tr>
		<tr>
			<td>SQLite</td>
			<td data-label="Setup">Embedded database library</td>
			<td data-label="Persistence">Yes</td>
			<td data-label="Queries">Full SQL</td>
		</tr>
		<tr>
			<td>Postgres</td>
			<td data-label="Setup">Database server</td>
			<td data-label="Persistence">Yes</td>
			<td data-label="Queries">Full SQL</td>
		</tr>
		<tr>
			<td>Mnesia</td>
			<td data-label="Setup">Included in OTP; configure a schema</td>
			<td data-label="Persistence">Yes</td>
			<td data-label="Queries">Key lookup, query expressions</td>
		</tr>
	</tbody>
</table>

Use DETS when you need persistent key lookup without a separate database service. Each table has a 2 GB file size limit. Disk-backed access is slower than ETS for frequent reads. See [Limitations](/advanced/limitations/).

## Pick a table type

The table modules share a core API. Set and bag tables also provide `insert_new`, with different duplicate rules. Only set tables provide `update_counter`.

<CardGrid>
	<LinkCard
		title="Set tables"
		href="/guides/set-tables/"
		description="One value per key. Inserting with an existing key replaces its value. Use for caches, configuration, and key lookup."
	/>
	<LinkCard
		title="Bag tables"
		href="/guides/bag-tables/"
		description="Multiple distinct values per key. For tags, categories, and one-to-many relationships."
	/>
	<LinkCard
		title="Duplicate bag tables"
		href="/guides/duplicate-bag-tables/"
		description="Store repeated key-value pairs. Useful for repeated events or measurements. Results are not ordered."
	/>
</CardGrid>

<a id="ready-to-try-it"></a>

## Next steps

slate follows [Semantic Versioning](/advanced/stability/). Continue with the [quick start](/quick-start/) to create a table, write values, and read them back.
